import {
    atom
} from "jotai";
export const categoryDialogToolsAtom = atom<string[]>([]),
    categoryDialogListNameAtom = atom(""),
    categoryDialogOpenAtom = atom(false),
    removeDialogOpenAtom = atom(false);
