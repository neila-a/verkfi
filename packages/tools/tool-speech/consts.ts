export const SpeechingWatershedWave = 15, // if it didn't speech in 15 seconds, it throws a warning
    defaultSpeechTime = 600,
    secondDivMS = 1000,
    vibrateTimes = 4,
    vibrateTime = 250,
    emptyBlobContext = ["Why you downloaded this??? Do not do it!!!"],

    // 试验得到的常数
    NoiseVoiceWatershedWave = 20; // origin 2.3
export type RecordingStateWithResume = RecordingState | "resume";
